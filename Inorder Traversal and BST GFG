class Solution{
    public:
    static bool ignore_case(int a,int b)
    {
        return (a<=b);
    }
    int isRepresentingBST(int arr[], int N)
    {
        if(is_sorted(arr,arr+N,ignore_case))
        {
            return 1;
        }
        return 0;
    }
};
